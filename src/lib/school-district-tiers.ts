// =====================================================================
// 校区濃淡 tier(1..4) のラベル定数（単一の真実源）。
//   校区ランキング表（CustomersClient）と地図の凡例・詳細パネルが
//   同じラベルを参照するために切り出した。片方だけ文言を変えるとズレるため、
//   ラベルはここ1箇所で定義する。
//   ⛔ 文言を変えない（4=非常に多い / 3=多い / 2=やや多い / 1=少ない）。
//   ※ 塗り色（TIER_CHIP は表側 / TIER_FILL は地図側）は各所に置く。ラベルのみ共通化。
// =====================================================================

export const TIER_LABEL: Record<number, string> = {
  4: '非常に多い',
  3: '多い',
  2: 'やや多い',
  1: '少ない',
}

// 濃淡データが無い（k=5 抑止・該当反響なし）校区の凡例文言（逐語・単一の真実源）。
//   地図の凡例／詳細パネル／PDF 出力が同じ文字列を参照する。閾値そのものは書かない。
export const NO_DATA_LEGEND = '件数が少ないため表示していません'
